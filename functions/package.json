{
  "name": "genuino-funciones",
  "description": "El portero de las salas de voz. Firma quién puede entrar y quién puede hablar.",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "revisar": "node --check index.js && node revisar-permiso.mjs",
    "desplegar": "npx firebase deploy --only functions --project genuino-host"
  },
  "dependencies": {
    "agora-token": "^2.0.5",
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^6.6.0"
  }
}
