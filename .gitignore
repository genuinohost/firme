node_modules/
dist/
*.local
.env*
firebase-debug.log*
.firebase/
.DS_Store
dev-dist/
Firme.apk
public/Firme.apk

# La clave de la cuenta de servicio de Firebase. Vive en ~/.firebase, fuera de
# aqui, pero si alguna vez se copia al repositorio por descuido esto la para:
# es una credencial con permiso de administrador sobre el proyecto entero.
*-firebase-adminsdk-*.json
genuino-despliegue.json
